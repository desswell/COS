# Python
__pycache__/
*.py[cod]
*.egg-info/
.pytest_cache/
.coverage
.ipynb_checkpoints/

# Data (too big for git)
/data/

# Large model weights and binary cm arrays — but keep CSV logs, JSON, PNG figures
*.pt
*.npy
results/smoke_*/

# Keep summary outputs and per-fold CSV/JSON logs (small, useful for report).
# Figures (PNG) and per-fold CSV are intentionally NOT ignored.
!results/.gitkeep

# Editors
.idea/
.vscode/
*.swp
